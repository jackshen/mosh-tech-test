import React from "react";

import Hero from "./Hero";

const Landing = () => {
  return (
    <div>
      <Hero />
      {/* Can add more here for the landing page */}
    </div>
  );
};

export default Landing;
